adc - Application Default Credentials Manager
===

```
adc list
adc current
adc use <CREDENTIAL_NAME>
adc add <CREDENTIAL_FILE>
adc exec <CREDENTIAL_NAME> <COMMAND> : run any arbitrary command in a subshell with the desired credential
adc login
```
